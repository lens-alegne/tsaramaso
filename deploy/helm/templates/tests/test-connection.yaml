apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tsaramaso.fullname" . }}-test-connection"
  labels:
    {{- include "tsaramaso.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tsaramaso.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
