apiVersion: v1
kind: Service
metadata:
  name: {{ include "tsaramaso.fullname" . }}
  labels:
    {{- include "tsaramaso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tsaramaso.selectorLabels" . | nindent 4 }}
